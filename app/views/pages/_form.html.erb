<%= simple_form_for @comment do |f| %>
<div class="form-group">
  <%= f.input :name, class: "form-control" %>
</div>
<div class="form-group">
  <%= f.input :description %>
</div>

  <%= f.button :submit %>

<% end %>
